# DESIGN_LOG.md
GraRec 設計判断記録

このファイルは、GraRecの各機能をなぜ・どのように実装したかを記録するものです。
実装のたびに追記していきます。卒論の考察・システム説明・今後の引き継ぎに使用します。

---

## 記録テンプレート（コピーして使用）

```
## [YYYY-MM-DD] 機能名

### 課題・背景
（なぜこれが必要になったか）

### 採用した設計
（具体的に何をどう実装したか）

### 根拠・参考にしたもの
- 先行研究：（あれば、RELATED_WORK.mdの該当項目を参照）
- 技術的な理由：
- 実験・検証結果：

### 検討したが採用しなかった案
（あれば）

### 関連するファイル・コミット
- ファイル：
- コミット：
```

---

## これまでの主な設計判断（時系列）

### [2026-07-XX] タイムラプス機能の実装

### 課題・背景
実験当日、研究者が現地不在のため、後から班ごとの画面推移を確認する手段が必要だった。

### 採用した設計
一定間隔で画面（キャンバス部分）をスクリーンショット化し、サーバー側にsession_idごとに時系列で保存する仕組みを実装。

### 根拠・参考にしたもの
- 技術的な理由：既存のPNG保存機能（html2canvas）を流用
- 実験・検証結果：後日、実装が抜け落ちていたことが判明し再実装（詳細は本ログの別項目参照）

---

### [2026-07-XX] AI支援あり/なし条件の設計変更（バックグラウンド処理化）

### 課題・背景
「支援なし」条件でも、後から比較分析ができるよう、AI処理自体は継続したかった。

### 採用した設計
「支援なし」条件でも録音・文字起こし・キーワード抽出は裏側で継続するが、話し合い中は画面に一切表示しない。振り返り画面で初めて開示する設計とした。これにより支援あり/なし両条件で取得データの粒度を統一。

### 根拠・参考にしたもの
- 先行研究：Argyle et al.（2023）の統制群設計の考え方（RELATED_WORK.md参照）

---

### [2026-07-XX] 話者分離をリアルタイム処理から除外

### 課題・背景
実験当日、Renderサーバーでメモリ不足（2GB超過）が発生し、502エラーでサービスが落ちた。原因はpyannote（話者分離モデル）のメモリ消費だった。

### 採用した設計
3分ごとの自動処理から話者分離を除外し、文字起こし・キーワード抽出のみに軽量化。話者分離は録音データを使って後日オフラインで実施する方針に変更。

### 検討したが採用しなかった案
- Gunicornのワーカー数を1に減らす（試したが根本解決にならず）
- Renderのプランをアップグレードする（コストの都合で見送り）

---

### [2026-07-XX] カード生成間隔の変更（3分固定→最初3分・以降10分）

### 課題・背景
3分間隔だとカードが出すぎて情報過多になり、また短い間隔では重要度の判断が浅くなる。

### 採用した設計
最初の1回目は3分後に実行（早期にシステムが動作していることを示す）、2回目以降は10分間隔に変更。10分間の会話から重要度の高い発言を優先してカード化する。

### 根拠・参考にしたもの
- 実験・検証結果：simulate_offline.pyによるオフライン検証で、頻度ベース（案A）・AI判断（案B）・組み合わせ（案C）を比較。案Bが最も安定して質の良いカードを生成することを確認

### 検討したが採用しなかった案
- 案A（発言頻度に基づく重要度判定）：フレーズの機械的な重複検出に留まり、意味的な重要度とズレていたため不採用
- 案C（A+Bの組み合わせ）：案Bとほぼ同じ結果になり、頻度による差別化の効果が薄かったため見送り

---

### [2026-07-XX] キーワード抽出プロンプトにシステム操作除外の指示を追加

### 課題・背景
オフライン検証で、GraRec自体への感想（「重要なとこだけキーワードにしてくれてるのはすごい」等）やUI操作の発言（「押してみて」等）が、企画内容と区別されずカード化されてしまう問題が見つかった。

### 採用した設計
build_prompt関数に、「GraRecというシステム自体の操作方法・使い方・感想についての発言は抽出対象から除外する」指示を追加。判定基準は「発言が量子力学の企画内容に言及しているか」とした。

### 根拠・参考にしたもの
- 実験・検証結果：simulate_offline.pyで班1・班5のデータを用いて改善前後を比較

---

### [2026-07-XX] 音声処理の分離（文字起こしとキーワード抽出のタイミングを分ける）

### 課題・背景
10分間隔でまとめて「文字起こし→キーワード抽出」を実行すると、1回あたりの処理が重くなる。また「量子力学」等の専門用語がWhisperで誤認識される問題があった。

### 採用した設計
文字起こしは短い間隔（1〜2分ごと）で先に実行してテキストを蓄積し、キーワード抽出（AIによる重要度判断）は10分ごとにまとめて実行する形に分離。あわせてWhisper APIの`prompt`パラメータに専門用語（量子力学、シュレーディンガーの猫等）のヒントを追加。

### 根拠・参考にしたもの
- 実験・検証結果：改善前後でsimulate_offline.pyを実行し比較。「量子力学」の誤認識（「漁師系」「糧酸」等）が減少したことを確認。一方でヒントの影響と思われる別の幻覚（無関係な定型文の繰り返し）も新たに発見された

### 今後の課題
- 複数班同時実施による周囲の雑音混入（マイクの混線）が、Whisperの幻覚を誘発するケースがあることを確認。次回実験に向けて、他の音声認識サービス（gpt-4o-transcribe-diarize、Deepgram、AssemblyAI等）の比較検証を予定
