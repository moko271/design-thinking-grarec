# DESIGN_LOG.md
GraRec 設計判断記録

このファイルは、GraRecの各機能をなぜ・どのように実装したかを記録するものです。
実装のたびに追記していきます。卒論の考察・システム説明・今後の引き継ぎに使用します。

---

## 記録テンプレート（コピーして使用）

```
## [YYYY-MM-DD] 機能名

### 課題・背景
（なぜこれが必要になったか）

### 採用した設計
（具体的に何をどう実装したか）

### 根拠・参考にしたもの
- 先行研究：（あれば、RELATED_WORK.mdの該当項目を参照）
- 技術的な理由：
- 実験・検証結果：

### 検討したが採用しなかった案
（あれば）

### 関連するファイル・コミット
- ファイル：
- コミット：
```

---

## これまでの主な設計判断（時系列）

### [2026-07-XX] タイムラプス機能の実装

### 課題・背景
実験当日、研究者が現地不在のため、後から班ごとの画面推移を確認する手段が必要だった。

### 採用した設計
一定間隔で画面（キャンバス部分）をスクリーンショット化し、サーバー側にsession_idごとに時系列で保存する仕組みを実装。

### 根拠・参考にしたもの
- 技術的な理由：既存のPNG保存機能（html2canvas）を流用
- 実験・検証結果：後日、実装が抜け落ちていたことが判明し再実装（詳細は本ログの別項目参照）

---

### [2026-07-XX] AI支援あり/なし条件の設計変更（バックグラウンド処理化）

### 課題・背景
「支援なし」条件でも、後から比較分析ができるよう、AI処理自体は継続したかった。

### 採用した設計
「支援なし」条件でも録音・文字起こし・キーワード抽出は裏側で継続するが、話し合い中は画面に一切表示しない。振り返り画面で初めて開示する設計とした。これにより支援あり/なし両条件で取得データの粒度を統一。

### 根拠・参考にしたもの
- 先行研究：Argyle et al.（2023）の統制群設計の考え方（RELATED_WORK.md参照）

---

### [2026-07-XX] 話者分離をリアルタイム処理から除外

### 課題・背景
実験当日、Renderサーバーでメモリ不足（2GB超過）が発生し、502エラーでサービスが落ちた。原因はpyannote（話者分離モデル）のメモリ消費だった。

### 採用した設計
3分ごとの自動処理から話者分離を除外し、文字起こし・キーワード抽出のみに軽量化。話者分離は録音データを使って後日オフラインで実施する方針に変更。

### 検討したが採用しなかった案
- Gunicornのワーカー数を1に減らす（試したが根本解決にならず）
- Renderのプランをアップグレードする（コストの都合で見送り）

---

### [2026-07-XX] カード生成間隔の変更（3分固定→最初3分・以降10分）

### 課題・背景
3分間隔だとカードが出すぎて情報過多になり、また短い間隔では重要度の判断が浅くなる。

### 採用した設計
最初の1回目は3分後に実行（早期にシステムが動作していることを示す）、2回目以降は10分間隔に変更。10分間の会話から重要度の高い発言を優先してカード化する。

### 根拠・参考にしたもの
- 実験・検証結果：simulate_offline.pyによるオフライン検証で、頻度ベース（案A）・AI判断（案B）・組み合わせ（案C）を比較。案Bが最も安定して質の良いカードを生成することを確認

### 検討したが採用しなかった案
- 案A（発言頻度に基づく重要度判定）：フレーズの機械的な重複検出に留まり、意味的な重要度とズレていたため不採用
- 案C（A+Bの組み合わせ）：案Bとほぼ同じ結果になり、頻度による差別化の効果が薄かったため見送り

---

### [2026-07-XX] キーワード抽出プロンプトにシステム操作除外の指示を追加

### 課題・背景
オフライン検証で、GraRec自体への感想（「重要なとこだけキーワードにしてくれてるのはすごい」等）やUI操作の発言（「押してみて」等）が、企画内容と区別されずカード化されてしまう問題が見つかった。

### 採用した設計
build_prompt関数に、「GraRecというシステム自体の操作方法・使い方・感想についての発言は抽出対象から除外する」指示を追加。判定基準は「発言が量子力学の企画内容に言及しているか」とした。

### 根拠・参考にしたもの
- 実験・検証結果：simulate_offline.pyで班1・班5のデータを用いて改善前後を比較

---

### [2026-07-XX] 音声処理の分離（文字起こしとキーワード抽出のタイミングを分ける）

### 課題・背景
10分間隔でまとめて「文字起こし→キーワード抽出」を実行すると、1回あたりの処理が重くなる。また「量子力学」等の専門用語がWhisperで誤認識される問題があった。

### 採用した設計
文字起こしは短い間隔（1〜2分ごと）で先に実行してテキストを蓄積し、キーワード抽出（AIによる重要度判断）は10分ごとにまとめて実行する形に分離。あわせてWhisper APIの`prompt`パラメータに専門用語（量子力学、シュレーディンガーの猫等）のヒントを追加。

### 根拠・参考にしたもの
- 実験・検証結果：改善前後でsimulate_offline.pyを実行し比較。「量子力学」の誤認識（「漁師系」「糧酸」等）が減少したことを確認。一方でヒントの影響と思われる別の幻覚（無関係な定型文の繰り返し）も新たに発見された

### 今後の課題
- 複数班同時実施による周囲の雑音混入（マイクの混線）が、Whisperの幻覚を誘発するケースがあることを確認。次回実験に向けて、他の音声認識サービス（gpt-4o-transcribe-diarize、Deepgram、AssemblyAI等）の比較検証を予定

---

### [2026-07-23] /quantum へのカード入れ替え・重複対策の展開

### 課題・背景
7/15実験後のアンケートで、複数の班から以下の不具合が報告された。
- カードの表示位置がフェーズの進行とともに画面下方へずれていき、最終的に視認できなくなる
- カードが一度に大量に表示され、情報過多になる

/mendan（面談用ワークシート）では「ユーザーが動かさなかったカードを次のサイクル開始前にアーカイブし、新しいカードに入れ替える」仕組みがすでに動作しており、同様の問題を解消している。この仕組みを /quantum にも展開した。

### 採用した設計

**（1）アーカイブ処理の /quantum 対応（`static/index_ai.html`）**

`onstop` 内の `doExtract` ブロック先頭で、カード退避ロジックを `mendanMode` / `quantumMode` で分岐する形に拡張した。

- `mendanMode`: 従来通り `data-area="unplaced"` のカードをすべてアーカイブ
- `quantumMode`: `data-area="unplaced"` かつ `data-source !== "manual"` かつ未アーカイブのカードのみを対象とし、AI生成カードのみ退避する

手動追加カード（`data-source="manual"`）は対象外とすることで、「支援なし」条件での手動カード追加と共存できる。

退避したカードには `data-archived="true"` と `display:none` を付与する。`session.notes` はすべてのノードを収集するため、アーカイブ済みカードも振り返り画面（`review.html`）に表示される。

**（2）`getNextUnplacedPosition()` の修正（`static/index_ai.html`）**

変更前: `document.querySelectorAll('#canvas .note[data-area="unplaced"]')` でアーカイブ済みも含む全 unplaced カードをカウントしていたため、退避後も位置番号が下方にずれ続けていた。

変更後: `:not([data-archived="true"])` を追加し、アーカイブ済みカードをカウントから除外した。これにより新しいカードは毎回 unplaced ゾーンの左上から詰めて並ぶ。この修正は /mendan にも副次的に適用される（既存動作への影響なし）。

### 根拠・参考にしたもの
- 実験・検証結果：7/15実験のアンケートデータ（複数班で「カードが見えなくなった」「どれを見ればいいかわからなかった」等の回答）
- 既存実装の流用：/mendan で動作確認済みの `data-archived` フラグ管理・セッション保存の仕組みをそのまま利用

### 検討したが採用しなかった案
- カード表示上限を設けてそれ以上生成しない：新しい発言に関するカードが出なくなるため不採用
- カードサイズを縮小して密に表示する：可読性が低下するため不採用
- フェーズ切り替え時に全カードをクリアする：振り返り画面での利用ができなくなるため不採用

### 関連するファイル・コミット
- ファイル：`static/index_ai.html`（`getNextUnplacedPosition` / `onstop` 内アーカイブブロック）

---

### [2026-07-28] メモリ使用量比較検証：pyannote vs gpt-4o-transcribe-diarize（検証中）

### 課題・背景
7/15実験当日、pyannote.audioによる話者分離がRenderサーバーのメモリ（2GB）を超過し、
502エラーでサービスが落ちた。その場の対応としてリアルタイム処理から話者分離を除外したが、
次回実験では話者分離を復活させたい。OpenAIのgpt-4o-transcribe-diarize（クラウドAPIで
話者分離を行う方式）に切り替えることで、サーバー側のメモリ消費を大幅に削減できると
仮説を立て、定量的に比較検証することにした。

### 採用した設計（検証方法）
`compare_memory_usage.py` を作成し、同一音声ファイルに対して以下の2方式を実行し比較する。

- **方式A（pyannote）**: `pyannote/speaker-diarization-3.1` パイプラインをロードして実行。
  モデルの重みをメモリ上に展開するため、ロード時に大量のRSSを消費する。
- **方式B（gpt-4o-transcribe-diarize）**: OpenAI APIに音声を送り、クラウド側で
  話者分離付き文字起こしを行う。サーバー側で行うのはHTTPリクエストのみなので、
  メモリ消費は最小限のはず。

psutilのRSSをバックグラウンドスレッドで100ms間隔でサンプリングし、ピーク値を記録する。
openai SDK 1.12.0はgpt-4o-transcribe-diarize未対応のため、httpxで直接
`/v1/audio/transcriptions` を呼び出す実装とした（SDK非依存）。

### 測定結果（未測定 → 実行後に記入）

| 指標               | 方式A (pyannote) | 方式B (gpt-4o-diarize) |
|--------------------|-----------------|------------------------|
| ベースラインRSS (MB) | 未測定          | 未測定                  |
| ピークRSS (MB)      | 未測定          | 未測定                  |
| 増加量 ΔRSS (MB)   | 未測定          | 未測定                  |
| 処理時間 (秒)       | 未測定          | 未測定                  |
| 検出話者数          | 未測定          | 未測定                  |

Render Shellでの実行コマンド:
```bash
# 方式Bのみ（pyannote未インストールの場合）
python compare_memory_usage.py data/recordings/<対象ファイル>.webm --skip-pyannote --output /tmp

# 方式A・B両方
HF_TOKEN=<トークン> python compare_memory_usage.py <ファイル> --output /tmp
```

### 根拠・参考にしたもの
- 実験当日の502エラーログ（pyannoteのメモリ消費が原因）
- OpenAI gpt-4o-transcribeファミリーのAPI仕様

### 検討したが採用しなかった案
- tracemalloc による Python ヒープ計測：RSS全体（モデルウェイトのC拡張メモリを含む）を
  捉えられないため不採用
- memory_profiler デコレータ：Render環境でのインストール不要にするためpsutilを選択

### 関連するファイル・コミット
- ファイル：`compare_memory_usage.py`
